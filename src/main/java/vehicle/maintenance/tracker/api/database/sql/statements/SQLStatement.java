package vehicle.maintenance.tracker.api.database.sql.statements;

/**
 * A enum of SQL statement commands used in the api
 *
 * @author Daile Alimo
 * @since 0.1-SNAPSHOT
 */
public enum SQLStatement {
    CREATE, SELECT, DELETE, INSERT, UPDATE, TRUNCATE
}
