package vehicle.maintenance.tracker.api.database.sql.statements;

/**
 * A enum of SQL data types used in the api
 *
 * @author Daile Alimo
 * @since 0.1-SNAPSHOT
 */
public enum SQLDataType {
    INT, VARCHAR, DATE, BLOB, FLOAT
}
